# Design direction (Stage 3A)

WatchAtlas is UI-heavy, so Stage 3A applies per `docs/sdlc-sync-contract.md`. This directory is
where that stage's artifacts belong once produced:

- A style-lock recording the chosen design direction (see `design-style-matrix.md`'s mandatory
  5-style comparison gate).
- A decisions log for design-specific calls, distinct from the general `docs/DECISIONS.md`.

This is a bookkeeping stub only — no design direction has been chosen or authored yet. Filling
it in is a genuine Stage 3A content gap, not something this sync fabricates.
